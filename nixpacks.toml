[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = ["python3 -m ensurepip", "python3 -m pip install --upgrade pip", "python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 run.py"